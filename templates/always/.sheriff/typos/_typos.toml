# Typos configuration
# Docs: https://github.com/crate-ci/typos

[default]
binary = false

[files]
extend-exclude = [
{% ListText(infra.exclude)|EachFormatted("%s/")|EachFormatted('    "%s",')|Joined("\n") %}
]
