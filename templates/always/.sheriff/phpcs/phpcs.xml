<?xml version="1.0"?>
<ruleset name="Sheriff PHPCS Rules">
    <description>Structural and semantic PHP rules</description>

{% ListText(php.src)|EachFormatted("../../%s")|EachFormatted("    <file>%s</file>")|Joined("\n") %}
{% ListText(php.tests)|EachFormatted("../../%s")|EachFormatted("    <file>%s</file>")|Joined("\n") %}

{% ListText(php.exclude)|EachFormatted("%s/*")|EachFormatted("    <exclude-pattern>%s</exclude-pattern>")|Joined("\n") %}

    <arg name="extensions" value="php"/>
    <arg name="colors"/>

    <rule ref="Generic.PHP.Syntax"/>

    <rule ref=".sheriff/phpcs/slevomat-types.xml"/>
    <rule ref=".sheriff/phpcs/slevomat-classes.xml"/>
    <rule ref=".sheriff/phpcs/slevomat-functions.xml"/>
    <rule ref=".sheriff/phpcs/slevomat-namespaces.xml"/>
    <rule ref=".sheriff/phpcs/slevomat-flow.xml"/>
    <rule ref=".sheriff/phpcs/slevomat-style.xml"/>

{% StringText(phpcs.extend) %}
</ruleset>
